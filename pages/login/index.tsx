import { NextPage } from "next";

const Login: NextPage = () => {
  return (
    <article>
      <form>
        <button type="submit">로그인</button>
      </form>
    </article>
  );
};

export default Login;
