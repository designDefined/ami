const noUser = "NO_USER" as const;
const noAtom = "NO_ATOM" as const;
const noPage = "NO_PAGE" as const;
const noProject = "NO_PROJECT" as const;
const admin = "ADMIN" as const;

const codes = { noUser, noAtom, noPage, noProject, admin };

export default codes;
